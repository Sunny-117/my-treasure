import React from 'react'

function AddTab() {
    return (
        <div>
            <button onClick={() => {
                console.log('commt')
            }}>增加tab
            </button>
        </div>
    )
}

export default AddTab